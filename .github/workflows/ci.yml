name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  model:
    name: Model tests and manifest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: "22"

      - name: Model tests
        run: node test/model.test.js

      - name: Manifest is valid JSON
        run: jq empty manifest.json

      - name: Manifest has the fields the marketplace requires
        run: |
          for field in schemaVersion id name version author description kinds entryPoints; do
            jq -e "has(\"$field\")" manifest.json >/dev/null \
              || { echo "manifest.json is missing required field: $field"; exit 1; }
          done

      - name: Manifest version matches the changelog
        run: |
          version=$(jq -r .version manifest.json)
          grep -q "^## \[$version\]" CHANGELOG.md \
            || { echo "CHANGELOG.md has no entry for version $version"; exit 1; }

      - name: Declared entry points exist
        run: |
          jq -r '.entryPoints | to_entries[] | .value' manifest.json | while read -r file; do
            test -f "$file" || { echo "entry point file not found: $file"; exit 1; }
          done

      - name: Plugin ID is namespaced and not reserved
        run: |
          id=$(jq -r .id manifest.json)
          case "$id" in
            omarchy.*) echo "third-party plugins may not use the omarchy.* namespace"; exit 1 ;;
            *.*) ;;
            *) echo "plugin id must be namespaced, e.g. author.plugin"; exit 1 ;;
          esac

      - name: No symlinks in the plugin folder
        run: |
          found=$(find . -path ./.git -prune -o -type l -print)
          test -z "$found" || { echo "plugin folders may not contain symlinks:"; echo "$found"; exit 1; }

  qml-syntax:
    name: QML syntax
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      # qmlformat parses QML without resolving imports, so this catches syntax
      # errors on a plain runner. Type checking needs Quickshell and the
      # Omarchy shell, which is what the qml-lint job below is for.
      - name: Install Qt 6 QML tooling
        run: |
          sudo apt-get update
          sudo apt-get install -y qt6-declarative-dev-tools

      - name: Parse QML
        run: |
          qmlformat=$(command -v qmlformat6 || command -v /usr/lib/qt6/bin/qmlformat || true)
          test -n "$qmlformat" || { echo "Qt 6 qmlformat not found"; exit 1; }
          echo "using $qmlformat"
          for file in Panel.qml Service.qml NetbirdIcon.qml; do
            "$qmlformat" "$file" >/dev/null || { echo "failed to parse $file"; exit 1; }
            echo "parsed $file"
          done

  qml-lint:
    name: QML lint
    runs-on: ubuntu-latest
    # Arch ships both Qt 6 and Quickshell, which this plugin's imports need.
    # Ubuntu has no Quickshell package, so type resolution fails there and
    # every symbol is reported as unknown.
    container: archlinux:latest
    # Not a gate yet: this job pins an upstream branch and a distro image that
    # can both move. Drop this line once it has been green for a while.
    continue-on-error: true
    steps:
      - name: Install Qt 6, Quickshell and tools
        run: pacman -Syu --noconfirm --needed qt6-declarative quickshell git

      - uses: actions/checkout@v5
        with:
          path: plugin

      - name: Check out the Omarchy shell
        uses: actions/checkout@v5
        with:
          repository: basecamp/omarchy
          ref: quattro
          path: omarchy

      - name: Lint QML against the shell
        run: |
          # `import qs.Ui` resolves as <import path>/qs/Ui/qmldir, so the shell
          # has to be reachable under a directory named `qs`.
          mkdir -p lintroot
          ln -sfn "$GITHUB_WORKSPACE/omarchy/shell" lintroot/qs
          /usr/lib/qt6/bin/qmllint -I lintroot -I "$GITHUB_WORKSPACE/omarchy/shell" \
            plugin/Panel.qml plugin/Service.qml plugin/NetbirdIcon.qml
